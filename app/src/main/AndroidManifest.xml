<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.co.blogbasbas.belajartoastalert">

    <!-- //tambahkan permission disini -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SPlashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IntroActivity"
            android:label=" Intro Activity" />
        <activity
            android:name=".WebViewActivity"
            android:label="Web View" />
        <activity
            android:name=".WebViewInternalActivity"
            android:label="Web Internal" />
        <activity android:name=".PersegiActivity"></activity>
    </application>

</manifest>